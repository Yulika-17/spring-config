server:
  port: 23595

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

first-service:
  url: ${FIRST_SERVICE_URL:http://localhost}
  port: ${FIRST_SERVICE_PORT:8083}
